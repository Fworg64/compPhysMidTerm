#include "renderer.h"

void generateRaysFromCamera(camera cam, tracingRay * rays, unsigned int numRays)
{
   //have camera pose and euler angles, need resolution and size of pixels 
   //and have distance of film from pinhole as zoom
}

void render(camera cam, triangularSurface * surfs, unsigned int numSurfs, outputimage)
{

}